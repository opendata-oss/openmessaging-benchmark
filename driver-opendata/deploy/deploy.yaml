#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
# http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
#

- name: OpenData Benchmark Setup
  hosts: client
  connection: ssh
  become: true
  vars:
    java_version: "25"
    rust_version: "stable"
    opendata_repo: "https://github.com/opendata-oss/opendata.git"
    opendata_java_repo: "https://github.com/opendata-oss/opendata-java.git"
    benchmark_repo: "https://github.com/opendata-oss/openmessaging-benchmark.git"
    # Branch defaults (override via -e opendata_branch=xxx)
    opendata_branch: "main"
    opendata_java_branch: "main"
    benchmark_branch: "master"
    # Consumer config
    separate_reader: true
    # s3_bucket and aws_region must be passed via -e (see README)
  tasks:
    # System setup
    - name: Update system packages
      yum:
        name: "*"
        state: latest
      when: ansible_facts['os_family'] == "RedHat"

    - name: Install base packages
      yum:
        name:
          - git
          - gcc
          - gcc-c++
          - make
          - openssl-devel
          - wget
          - vim
          - htop
          - sysstat
        state: present
      when: ansible_facts['os_family'] == "RedHat"

    # Java installation via Eclipse Temurin (Adoptium)
    - name: Add Adoptium GPG key
      rpm_key:
        key: https://packages.adoptium.net/artifactory/api/gpg/key/public
        state: present
      when: ansible_facts['os_family'] == "RedHat"

    - name: Add Adoptium yum repo
      yum_repository:
        name: Adoptium
        description: Eclipse Temurin packages from Adoptium
        baseurl: https://packages.adoptium.net/artifactory/rpm/rhel/9/$basearch
        gpgcheck: yes
        gpgkey: https://packages.adoptium.net/artifactory/api/gpg/key/public
      when: ansible_facts['os_family'] == "RedHat"

    - name: Install Temurin {{ java_version }}
      yum:
        name: "temurin-{{ java_version }}-jdk"
        state: present
      when: ansible_facts['os_family'] == "RedHat"

    - name: Set JAVA_HOME
      lineinfile:
        path: /etc/environment
        line: 'JAVA_HOME=/usr/lib/jvm/temurin-{{ java_version }}-jdk'
        create: yes

    # Maven installation
    - name: Download Maven
      get_url:
        url: https://archive.apache.org/dist/maven/maven-3/3.9.6/binaries/apache-maven-3.9.6-bin.tar.gz
        dest: /tmp/maven.tar.gz

    - name: Extract Maven
      unarchive:
        src: /tmp/maven.tar.gz
        dest: /opt
        remote_src: yes
        creates: /opt/apache-maven-3.9.6

    - name: Create Maven symlink
      file:
        src: /opt/apache-maven-3.9.6
        dest: /opt/maven
        state: link

    - name: Add Maven to PATH
      lineinfile:
        path: /etc/environment
        line: 'PATH=/opt/maven/bin:{{ ansible_facts["env"]["PATH"] }}'
        regexp: '^PATH='
        create: yes

    # Rust installation
    - name: Check if Rust is installed
      stat:
        path: /root/.cargo/bin/rustc
      register: rust_installed

    - name: Download Rustup
      get_url:
        url: https://sh.rustup.rs
        dest: /tmp/rustup.sh
        mode: '0755'
      when: not rust_installed.stat.exists

    - name: Install Rust
      shell: /tmp/rustup.sh -y --default-toolchain {{ rust_version }}
      args:
        creates: /root/.cargo/bin/rustc
      when: not rust_installed.stat.exists

    - name: Add Cargo to PATH for all users
      lineinfile:
        path: /etc/profile.d/cargo.sh
        line: 'export PATH="$PATH:/root/.cargo/bin"'
        create: yes
        mode: '0644'

    # Clone repositories
    - name: Clone opendata repository (native C library)
      git:
        repo: "{{ opendata_repo }}"
        dest: /opt/opendata
        version: "{{ opendata_branch }}"
        force: yes

    - name: Clone opendata-java repository
      git:
        repo: "{{ opendata_java_repo }}"
        dest: /opt/opendata-java
        version: "{{ opendata_java_branch }}"
        force: yes

    - name: Clone benchmark repository
      git:
        repo: "{{ benchmark_repo }}"
        dest: /opt/benchmark-src
        version: "{{ benchmark_branch }}"
        force: yes

    # Build native C library from the opendata Cargo workspace
    - name: Build opendata native C library
      shell: |
        source /root/.cargo/env
        cd /opt/opendata
        cargo build --release -p opendata-log-c
      args:
        executable: /bin/bash

    # Build opendata-java with Gradle and install to local Maven repo
    - name: Build opendata-java
      shell: |
        export JAVA_HOME=/usr/lib/jvm/temurin-{{ java_version }}-jdk
        cd /opt/opendata-java
        ./gradlew build -x test
      args:
        executable: /bin/bash

    - name: Install opendata-java common jar to local Maven repo
      shell: |
        export JAVA_HOME=/usr/lib/jvm/temurin-{{ java_version }}-jdk
        export PATH=/opt/maven/bin:$PATH
        mvn install:install-file \
          -Dfile=/opt/opendata-java/common/build/libs/common-0.1.0-SNAPSHOT.jar \
          -DgroupId=dev.opendata -DartifactId=common \
          -Dversion=0.1.0-SNAPSHOT -Dpackaging=jar
      args:
        executable: /bin/bash

    - name: Install opendata-java log jar to local Maven repo
      shell: |
        export JAVA_HOME=/usr/lib/jvm/temurin-{{ java_version }}-jdk
        export PATH=/opt/maven/bin:$PATH
        mvn install:install-file \
          -Dfile=/opt/opendata-java/log/build/libs/log-0.1.0-SNAPSHOT.jar \
          -DgroupId=dev.opendata -DartifactId=log \
          -Dversion=0.1.0-SNAPSHOT -Dpackaging=jar
      args:
        executable: /bin/bash

    # Build benchmark
    - name: Build benchmark
      shell: |
        export JAVA_HOME=/usr/lib/jvm/temurin-{{ java_version }}-jdk
        export PATH=/opt/maven/bin:$PATH
        cd /opt/benchmark-src
        mvn clean install -pl driver-opendata,package -am -DskipTests -Dcheckstyle.skip=true -Dspotless.check.skip=true -Dlicense.skip=true -Dspotbugs.skip=true
      args:
        executable: /bin/bash

    - name: Create benchmark directory
      file:
        path: /opt/benchmark
        state: directory

    - name: Extract benchmark package
      unarchive:
        src: /opt/benchmark-src/package/target/openmessaging-benchmark-0.0.1-SNAPSHOT-bin.tar.gz
        dest: /opt/benchmark
        remote_src: yes
        extra_opts: ["--strip-components=1"]

    # Set up native library path
    - name: Create ld.so.conf.d entry for native library
      copy:
        content: "/opt/opendata/target/release"
        dest: /etc/ld.so.conf.d/opendata.conf

    - name: Update ldconfig
      command: ldconfig

    # Remove workers.yaml to use LocalWorker mode
    - name: Remove workers.yaml for local mode
      file:
        path: /opt/benchmark/workers.yaml
        state: absent

    # Set JVM_OPTS for native library path and FFM native access
    - name: Configure JVM_OPTS for native library
      copy:
        content: 'export JVM_OPTS="-Djava.library.path=/opt/opendata/target/release --enable-native-access=ALL-UNNAMED"'
        dest: /etc/profile.d/opendata-jvm.sh
        mode: '0644'

    # Generate driver config from template
    - name: Generate OpenData driver config
      template:
        src: templates/driver-opendata.yaml
        dest: /opt/benchmark/driver-opendata.yaml

- name: Display connection info
  hosts: client
  connection: ssh
  tasks:
    - name: Show benchmark info
      debug:
        msg: |
          Benchmark client ready!
          SSH: ssh ec2-user@{{ inventory_hostname }}
          Run benchmark:
            cd /opt/benchmark
            sudo -E bin/benchmark --drivers driver-opendata.yaml workloads/1-topic-1-partition-1kb.yaml
